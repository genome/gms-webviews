<%= content_tag :li do
    link_to("/search?q=#{query}&fq=#{facet[0]}", class: facet[0] == @facet ? "active" : "") do 
      "<i class='#{icon_for_facet(facet[0])}'></i>#{facet[0]} (#{facet[1]})".html_safe
    end
  end
%>
