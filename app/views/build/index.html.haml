=render partial: 'shared/side_nav', locals: tab_hash
%table.table.table-bordered.gms-entity-index{id: 'builds-datatable', data: {source: build_index_url(format: :json)}}
  %thead
    %tr
      %th Build
      %th Status
      %th Model
  %tbody
