
<div class="well">
    <h1>Build <%= @build.build_id %></h1>
    <h3><%= @build.model.name %></h3>

    <br/>
    <%= render partial: 'build/summary', locals: { build: @build } %>

</div>

<div class="span12">
  <div class="page-header">
    <h2>Inputs</h2>
  </div>

    <table class="table table-bordered">
        <tbody>
      <% @inputs.each do |i| %>
        <tr>
            <td><%= i.name %></td>
            <td>
                <%= render_value_for_model_or_build_input(i) %>
            </td>
        </tr>
      <% end  %>
        </tbody>
    </table>
</div>

<div class="span12">
    <div class="page-header">
        <h2>Workflow</h2>
    </div>
    
    not in postgres yet.
</div>


