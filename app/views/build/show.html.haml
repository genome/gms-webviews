.page-header
  %h3= @build.model_name
  %h1
    =@build.build_id
    %small build
.well
  =render partial: 'shared/attribute_list', locals: { presenter: @build }

.span12
  .page-header
    %h2 Inputs
  =render partial: 'shared/attribute_list', locals: { presenter: @build.inputs }

.span12
  .page-header
    %h2 Workflow
  %p not in postgres yet.
